apiVersion: v1
kind: Pod
metadata:
  name: nap-locator
  labels:
    env: test
spec:
  containers:
  - name: nap-locator
    image: nginx
    imagePullPolicy: IfNotPresent
  nodeSelector:
    roles: worker
